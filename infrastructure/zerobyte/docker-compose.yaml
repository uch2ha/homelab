services:
  zerobyte:
    image: ghcr.io/nicotsx/zerobyte:${VERSION}
    container_name: zerobyte
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Europe/Helsinki
      - BASE_URL='$BASE_URL'
      - APP_SECRET='$APP_SECRET' # `openssl rand -hex 32`
      - WEBHOOK_ALLOWED_ORIGINS='$WEBHOOK_ALLOWED_ORIGINS'
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./zerobyte:/var/lib/zerobyte
      # read-only
      - /home:/home:ro
      - ${MAIN_1}:/hdd-main:ro
      # read-write
      - ${BACKUP_1}/home-backup:/home-backup
      - ${BACKUP_1}/immich-backup:/immich-backup
      - ${BACKUP_1}/papra-backup:/papra-backup
    healthcheck:
      test: wget --spider http://localhost:4096/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - homelab_infrastructure
    ports:
      - '4096:4096'

networks:
  homelab_infrastructure:
    external: true
